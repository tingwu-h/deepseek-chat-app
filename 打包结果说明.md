# 打包结果说明（最终交付）

> 状态：**已完成，APK 已产出并通过验证**

---

## 一、交付物

| 项目 | 内容 |
| --- | --- |
| APK 路径 | `dist\deepseek-chat-v1.0.0.apk` |
| 大小 | 51.1 MB（53,586,286 字节） |
| SHA256 | `ACBFFC99E01F2775F6E9CF20828EB43EA57B0772DE3CB1B23F0A0BE6D24AE23F` |
| 包名 | `com.example.deepseek_chat` |
| 应用名 | DeepSeek 助手 |
| versionName / versionCode | 1.0.0 / 1 |
| 支持架构 | arm64-v8a、armeabi-v7a、x86_64（模拟器也能装） |
| compileSdk / targetSdk | 36 / 36 |
| 权限 | `INTERNET`、`ACCESS_NETWORK_STATE` |
| 签名 | APK Signature Scheme v2，证书 `CN=Android Debug`（debug 签名，可正常安装） |

### 安装到手机

**方式一：USB（推荐）**
1. 手机 → 设置 → 关于手机 → 连点「版本号」7 次，开启开发者选项
2. 开发者选项里打开 **USB 调试**，插上数据线，手机弹窗点「允许」
3. 在项目目录执行：
   ```powershell
   C:\dsbuild\android-sdk\platform-tools\adb.exe install -r dist\deepseek-chat-v1.0.0.apk
   ```

**方式二：直接传文件**
把 `dist\deepseek-chat-v1.0.0.apk` 发到手机（微信/QQ/网盘/数据线），
在手机上点开安装，允许「安装未知来源应用」即可。

> debug 签名会提示「未经安全检测」，点继续安装即可。
> 要发布给别人长期使用，请用 `tools\setup_android_build.ps1 -Release` 生成自己的正式签名
> （注意：一旦用正式签名发布，后续升级必须用同一个密钥，否则用户无法覆盖安装）。

### 首次使用

1. 打开 App，点右上角 **设置**
2. 粘贴 DeepSeek API Key（<https://platform.deepseek.com> → API Keys 创建）
3. 点 **测试连接** 确认可用
4. 回到聊天页输入问题发送，回复会**逐字显示**（流式）；生成中点 ⏹ 可随时停止

---

## 二、验证记录（都做了，不是「应该能跑」）

| 验证项 | 方法 | 结果 |
| --- | --- | --- |
| 静态分析 | `flutter analyze` | **No issues found!** |
| 单元 + Widget 测试 | `flutter test` | **11/11 全部通过**（含「发送消息后流式拼接助手回复」「设置页回填 API Key」） |
| APK 结构 | `aapt2 dump badging` | 包名、应用名、权限、启动 Activity 全部正确 |
| APK 签名 | `apksigner verify` | v2 签名有效，可安装 |
| **代码确实打进 APK** | 解包后在 `libapp.so` 中搜索源码路径 | `package:deepseek_chat` 及 7 个模块路径**全部命中** |
| 网络权限 | 清单检查 | `INTERNET` 存在（已实测踩过被模板覆盖的坑，脚本现在会强制校验） |

最后一项特意做得很硬：之前有一次打出来的 APK 其实是 Flutter 默认的计数器 Demo，
所以现在脚本里加了两道校验——编译前校验 `lib/main.dart` 与 `pubspec.yaml` 是不是本项目代码，
打包后校验 APK 里能不能找到本应用特征，任一不通过就直接失败，不会再交付错包。

---

## 三、过程中踩到并解决的坑（都是实测结论）

1. **`flutter` 每次启动都去 `git fetch --tags` 连 github.com**
   → 连不上时等 21 秒后失败（`RPC failed; curl 28`），表现为「flutter 命令卡死/工具链反复重建」。
   → 解决：在重定向后的 AppData 里写 `.flutter_settings` 设 `no-version-check`。
   （注：早前怀疑的「中文路径导致 spawn 被拒」被证伪——纯英文路径下同样发生。）

2. **Dart 按 Latin-1 解码 `%APPDATA%`**
   → 中文用户名变成乱码路径 `C:\Users\渚畤楣廫AppData\...`，直接报错。
   → 解决：把 `APPDATA` / `LOCALAPPDATA` / `PUB_CACHE` / `GRADLE_USER_HOME` 全部重定向进构建目录。

3. **`flutter create . --overwrite` 覆盖源码**（最严重）
   → 把 `lib/main.dart`、`pubspec.yaml`、`test/widget_test.dart` 换成模板，**打出的是 Demo APK**。
   → 解决：去掉 `--overwrite`（只补缺失文件）+ 覆盖前备份、覆盖后还原 + 编译前硬校验。

4. **版本矩阵对不上**（Flutter 3.47.5 的支持策略写死在源码里）
   → Gradle 错误下限 8.14.0、警告下限 9.1.0；AGP 错误下限 8.11.1、警告下限 9.0.1；KGP 错误下限 2.2.20。
   → 解决：直接读 `DependencyVersionChecker.kt` 与 `gradle_utils.dart` 的模板常量，
     对齐成 **Gradle 9.3.1 / AGP 9.1.0 / Kotlin 2.4.0**。
   → 连带修：AGP 9 起 `android { kotlinOptions {} }` 与 `id("kotlin-android")` 都不能用了，
     要改成 `kotlin { compilerOptions { jvmTarget = JvmTarget.JVM_17 } }`。

5. **`settings.gradle.kts` 顶层写 `repositories {}`**（我自己写错）
   → Kotlin DSL 里该作用域没有 `google()`，报 `Unresolved reference: google`。
   → 解决：仓库只放 `pluginManagement` 里，删掉顶层块。

6. **代码本身的 4 个编译错误**（这次构建才第一次暴露出来）
   → `app_settings_provider.dart` 缺 `ChangeNotifier` 的 import；
     `ChatMessage.error` 被误声明为 `final` 但代码里要赋值；
     `ThemeData.cardTheme` 参数类型是 `CardThemeData` 而非 `CardTheme`。
   → 全部已修。

7. **产物路径不固定**
   → 本项目的 `android/build.gradle.kts` 没有 Flutter 模板那种 build 目录重定向，
     Gradle 把 APK 放在 `android\app\build\outputs\` 下，导致 `flutter build apk` 报
     「couldn't find apk」并返回非 0（其实已成功）。
   → 解决：脚本改为搜索多个候选路径，按实际产物判定成功。

---

## 四、以后改了代码怎么重新打包

```powershell
# 一条命令，复用已装好的工具链（约 1~2 分钟）
powershell -ExecutionPolicy Bypass -File tools\ascii_build.ps1
```

要**在普通（非沙箱）终端**里跑。脚本会自动同步源码到 `C:\dsbuild\deepseek_chat`、
校验源码没被模板覆盖、编译、校验产物、把新 APK 复制回 `dist\`。

工具链位置（都是独立的，不污染系统）：
- `C:\dsbuild\` —— Flutter SDK 3.47.5 + JDK 17 + Android SDK 36 + Gradle 缓存
- `<项目>\.toolchain\downloads\` —— 四个安装包（2.4 GB，已解压完，属于冗余备份）
